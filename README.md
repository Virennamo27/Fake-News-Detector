# Fake-News-Detector
A machine learning project that classifies news articles as fake or real using Natural Language Processing (NLP) and Logistic Regression. The model processes text data, balances classes using SMOTE, and applies TF-IDF vectorization to achieve over 96% accuracy on test data.
